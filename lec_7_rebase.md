```
// never run the rebase command while you are in master or main branch-----it is meant to be run in side branch

//keeping commit messages clean

git commit -am ---- shortcut for adding and commiting

git merge master--- run it on the other branch not in master to merge master in that branch

git rebase master --- run while being on another branch don't run it in master

//NOTE --- never rebase the commits you shared or pushed to gitHub

```