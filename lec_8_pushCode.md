```
//setup ssh keys to connect with github,github uses ssh to allow you to push code. password based code push is not allowed

// check instruction for your OS on github website, as that's best and updated resource

git remote -v ---- whether you have any remote repository

git remote add origin url --- 

git push -u origin main --- origin(remote name where we want to push) , main(branch), -u(sets upstream for future push that will directly push the code)

git remote rename oldname newname ---

git remote remove name --- change remote name

git clone <url> --- to bring that repo in our system



```