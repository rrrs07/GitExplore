## steps

```
ls --- to refresh

mkdir --- to create folders inside the directory

git status -- to know the current status(but shows fatal: not a git repository (or any of the parent directories): .git
)

cd and name of folder -- to access that folder

ls -la -- shows the hidden folders

git init -- to initialised

//after this run git status

git status
(On branch master
No commits yet
nothing to commit (create/copy files and use "git add" to track)
)

cd .. -- to go back




```